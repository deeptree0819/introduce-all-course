export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}

export enum AdminRole {
  SUPER = "SUPER",
  MANAGER = "MANAGER",
}

export enum Role {
  SUPER = "SUPER",
  MANAGER = "MANAGER",
  USER = "USER",
  EXPERT = "EXPERT",
}

export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum BannerStatus {
  BEFORE = "BEFORE",
  PROGRESS = "PROGRESS",
  AFTER = "AFTER",
}
